# 🎯 Roadmap: Упрощение бота (только Groq + OCR)

**Цель:** Убрать Gemini, оставить только Groq + Tesseract OCR для фото

---

## Этап 1: Создание нового чистого бота ✨

### 1.1 Создать новый файл bot_groq.py
```bash
cd /root/telegram-bot
nano bot_groq.py
```

### 1.2 Скопировать содержимое из GitHub
Файл будет создан в следующем коммите с полным кодом

### 1.3 Проверить что есть все зависимости
```bash
source venv/bin/activate
pip list | grep -E "aiogram|httpx|pytesseract|pillow"
```

Если чего-то нет:
```bash
pip install aiogram httpx pytesseract pillow
```

---

## Этап 2: Тестовый запуск нового бота 🧪

### 2.1 Остановить старый бот
```bash
pm2 stop school-bot
```

### 2.2 Запустить новый бот вручную
```bash
python3 bot_groq.py
```

### 2.3 Проверить в Telegram
- Отправить текстовое сообщение
- Отправить фото с текстом
- Проверить что бот отвечает быстро

### 2.4 Остановить тестовый запуск
```
Ctrl+C
```

---

## Этап 3: Замена старого бота на новый 🔄

### 3.1 Переименовать файлы
```bash
mv bot_with_logs.py bot_with_logs_old.py
mv bot_groq.py bot_with_logs.py
```

### 3.2 Перезапустить через PM2
```bash
pm2 restart school-bot
pm2 logs school-bot --lines 30
```

### 3.3 Проверить что всё работает
- Отправить несколько сообщений
- Отправить фото
- Проверить логи на ошибки

---

## Этап 4: Очистка и коммит 🧹

### 4.1 Удалить старый файл (если всё работает)
```bash
rm bot_with_logs_old.py
```

### 4.2 Закоммитить изменения
```bash
git add bot_with_logs.py
git commit -m "Simplify bot: remove Gemini, keep only Groq + OCR"
git push
```

### 4.3 Сохранить PM2 конфигурацию
```bash
pm2 save
```

---

## Что изменится ✅

**Убрано:**
- ❌ Gemini API и все ключи
- ❌ Команда /model (выбор модели)
- ❌ Переключение между моделями
- ❌ Сложная логика fallback
- ❌ Упоминания моделей в логах

**Остаётся:**
- ✅ Groq API для всех текстовых запросов
- ✅ Tesseract OCR для распознавания текста с фото
- ✅ История диалога
- ✅ Команды /start, /clear, /reset
- ✅ Упрощённый промпт (математика = структура, остальное = свободно)

---

## Ожидаемый результат 🎯

После выполнения:
- Бот работает только через Groq (быстро)
- Фото обрабатываются через OCR за 5-7 секунд
- Нет упоминаний моделей в интерфейсе
- Код в 2 раза короче и проще
- Промпт адаптируется под предмет (математика vs гуманитарные)

---

## Время выполнения ⏱️

- Этап 1: 5 минут
- Этап 2: 5 минут
- Этап 3: 2 минуты
- Этап 4: 3 минуты

**Итого:** ~15 минут
